<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shronda Jeanine & Company - Arizona Community Action Program</title>
    <meta name="description" content="Holistic wellness coaching and community action program serving homeless and underserved individuals and families in Phoenix, Arizona.">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Crimson+Text:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-color: #4A5568;
            --secondary-color: #718096;
            --accent-color: #805AD5;
            --warm-accent: #D69E2E;
            --sage-green: #68D391;
            --soft-blue: #63B3ED;
            --cream: #FFF8DC;
            --warm-white: #FEFEFE;
            --soft-gray: #F7FAFC;
            --text-dark: #2D3748;
            --text-light: #4A5568;
        }

        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.7;
            color: var(--text-dark);
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Navigation */
        .navbar {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 20px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .logo {
            font-family: 'Crimson Text', serif;
            font-size: 1.8rem;
            font-weight: 600;
            color: var(--accent-color);
            text-decoration: none;
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 2rem;
            align-items: center;
        }

        .nav-link {
            text-decoration: none;
            color: var(--text-dark);
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
            padding: 8px 16px;
            border-radius: 25px;
        }

        .nav-link:hover {
            color: var(--accent-color);
            background: rgba(128, 90, 213, 0.1);
        }

        .nav-link.crisis-btn {
            background: linear-gradient(135deg, #e53e3e, #c53030);
            color: white;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-size: 0.9rem;
        }

        .nav-link.crisis-btn:hover {
            background: linear-gradient(135deg, #c53030, #9c2626);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(197, 48, 48, 0.4);
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(135deg, rgba(128, 90, 213, 0.9), rgba(104, 211, 145, 0.9)), 
                        url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 600"><defs><filter id="noiseFilter"><feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="4" stitchTiles="stitch"/></filter></defs><rect width="100%" height="100%" filter="url(%23noiseFilter)" opacity="0.05"/></svg>');
            min-height: 100vh;
            display: flex;
            align-items: center;
            text-align: center;
            color: white;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="40" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="0.5"/><circle cx="50" cy="50" r="30" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="0.3"/><circle cx="50" cy="50" r="20" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="0.2"/></svg>') center/200px repeat;
            animation: float 20s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(180deg); }
        }

        .hero-content {
            position: relative;
            z-index: 2;
        }

        .hero h1 {
            font-family: 'Crimson Text', serif;
            font-size: clamp(2.5rem, 5vw, 4rem);
            margin-bottom: 1.5rem;
            font-weight: 400;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .hero-subtitle {
            font-size: clamp(1.1rem, 2vw, 1.4rem);
            margin-bottom: 2.5rem;
            opacity: 0.95;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .affirmation-container {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            padding: 2rem;
            margin: 2rem 0;
            border-radius: 15px;
            text-align: center;
            border-left: 5px solid var(--sage-green);
        }

        #daily-affirmation {
            font-family: 'Crimson Text', serif;
            font-size: 1.3rem;
            color: var(--accent-color);
            font-style: italic;
        }

        .cta-buttons {
            display: flex;
            gap: 1.5rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            display: inline-block;
            padding: 15px 35px;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-size: 0.9rem;
            border: none;
            cursor: pointer;
        }

        .btn-primary {
            background: rgba(255, 255, 255, 0.9);
            color: var(--accent-color);
            backdrop-filter: blur(10px);
        }

        .btn-primary:hover {
            background: white;
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        }

        .btn-secondary {
            background: transparent;
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.8);
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-3px);
        }

        .btn-crisis {
            background: linear-gradient(135deg, #e53e3e, #c53030);
            color: white;
            border: none;
        }

        .btn-crisis:hover {
            background: linear-gradient(135deg, #c53030, #9c2626);
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(197, 48, 48, 0.4);
        }

        /* Page Content */
        .page {
            display: none;
            min-height: 100vh;
            padding-top: 120px;
        }

        .page.active {
            display: block;
        }

        .page-header {
            text-align: center;
            margin-bottom: 4rem;
            padding: 3rem 0;
            background: var(--soft-gray);
        }

        .page-header h2 {
            font-family: 'Crimson Text', serif;
            font-size: 3rem;
            color: var(--accent-color);
            margin-bottom: 1rem;
        }

        .page-header p {
            font-size: 1.2rem;
            color: var(--text-light);
            max-width: 600px;
            margin: 0 auto;
        }

        /* Features Section */
        .features {
            padding: 4rem 0;
            background: var(--warm-white);
        }

        .features h3 {
            text-align: center;
            font-family: 'Crimson Text', serif;
            font-size: 2.5rem;
            color: var(--accent-color);
            margin-bottom: 3rem;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2.5rem;
        }

        .feature-card {
            background: white;
            padding: 2.5rem;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .feature-card i {
            font-size: 3rem;
            color: var(--accent-color);
            margin-bottom: 1.5rem;
        }

        .feature-card h4 {
            font-family: 'Crimson Text', serif;
            font-size: 1.5rem;
            color: var(--text-dark);
            margin-bottom: 1rem;
        }

        /* Services Section */
        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2.5rem;
            margin: 4rem 0;
        }

        .service-card {
            background: white;
            padding: 2.5rem;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            text-align: center;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(135deg, var(--accent-color), var(--sage-green));
        }

        .service-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .service-card i {
            font-size: 3rem;
            color: var(--accent-color);
            margin-bottom: 1.5rem;
        }

        .service-card h3 {
            font-family: 'Crimson Text', serif;
            font-size: 1.8rem;
            color: var(--accent-color);
            margin-bottom: 1rem;
        }

        .service-pricing {
            display: flex;
            gap: 1rem;
            margin: 1.5rem 0;
            flex-wrap: wrap;
            justify-content: center;
        }

        .price-option {
            background: var(--soft-gray);
            padding: 10px 15px;
            border-radius: 25px;
            font-size: 0.9rem;
            font-weight: 600;
            color: var(--text-light);
        }

        .price {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--warm-accent);
            margin: 1rem 0;
        }

        /* Community Events */
        .events-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin: 3rem 0;
        }

        .event-card {
            background: linear-gradient(135deg, var(--cream), var(--warm-white));
            padding: 2rem;
            border-radius: 15px;
            border-left: 5px solid var(--warm-accent);
            transition: all 0.3s ease;
            text-align: center;
        }

        .event-card:hover {
            transform: translateX(10px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        }

        .event-card i {
            font-size: 2.5rem;
            color: var(--warm-accent);
            margin-bottom: 1rem;
        }

        .event-card h3 {
            color: var(--warm-accent);
            margin-bottom: 1rem;
        }

        /* Testimonials */
        .testimonials-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin: 3rem 0;
        }

        .testimonial-card {
            background: white;
            padding: 2.5rem;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            text-align: center;
            position: relative;
        }

        .testimonial-card::before {
            content: '"';
            font-size: 4rem;
            color: var(--accent-color);
            position: absolute;
            top: -10px;
            left: 20px;
            font-family: 'Crimson Text', serif;
        }

        .stars {
            color: var(--warm-accent);
            font-size: 1.2rem;
            margin-bottom: 1rem;
        }

        .testimonial-card cite {
            font-style: italic;
            color: var(--text-light);
            margin-top: 1rem;
            display: block;
        }

        /* Donation Tiers */
        .donation-tiers {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin: 4rem 0;
        }

        .tier-card {
            background: white;
            padding: 3rem 2rem;
            border-radius: 20px;
            text-align: center;
            position: relative;
            transition: all 0.3s ease;
            border: 3px solid transparent;
        }

        .tier-card.featured {
            border-color: var(--accent-color);
            transform: scale(1.05);
        }

        .tier-card:hover {
            transform: translateY(-5px) scale(1.02);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .tier-title {
            font-family: 'Crimson Text', serif;
            font-size: 1.8rem;
            color: var(--accent-color);
            margin-bottom: 1rem;
        }

        .tier-price {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--warm-accent);
            margin-bottom: 1rem;
        }

        .tier-features {
            list-style: none;
            margin: 2rem 0;
            text-align: left;
        }

        .tier-features li {
            padding: 0.5rem 0;
            padding-left: 1.5rem;
            position: relative;
        }

        .tier-features li::before {
            content: '✦';
            position: absolute;
            left: 0;
            color: var(--sage-green);
        }

        /* Newsletter Section */
        .newsletter-section {
            background: linear-gradient(135deg, var(--accent-color), var(--sage-green));
            padding: 4rem 0;
            margin: 4rem 0;
            border-radius: 20px;
            color: white;
            text-align: center;
        }

        .newsletter-form {
            max-width: 500px;
            margin: 0 auto;
            display: flex;
            gap: 1rem;
            margin-top: 2rem;
        }

        .newsletter-form input {
            flex: 1;
            padding: 15px 20px;
            border: none;
            border-radius: 50px;
            font-size: 1rem;
        }

        .newsletter-form button {
            padding: 15px 30px;
            background: white;
            color: var(--accent-color);
            border: none;
            border-radius: 50px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .newsletter-form button:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        /* Contact Section */
        .contact-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 3rem;
            margin: 3rem 0;
        }

        .contact-info {
            background: var(--soft-gray);
            padding: 3rem;
            border-radius: 15px;
        }

        .contact-item {
            display: flex;
            align-items: center;
            margin-bottom: 1.5rem;
            font-size: 1.1rem;
        }

        .contact-item i {
            color: var(--accent-color);
            margin-right: 1rem;
            font-size: 1.2rem;
            width: 20px;
        }

        .contact-item a {
            color: var(--accent-color);
            text-decoration: none;
        }

        .contact-item a:hover {
            text-decoration: underline;
        }

        .contact-form {
            background: white;
            padding: 3rem;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        .contact-form input,
        .contact-form select,
        .contact-form textarea {
            width: 100%;
            padding: 15px;
            margin-bottom: 1.5rem;
            border: 2px solid #eee;
            border-radius: 10px;
            font-size: 1rem;
            transition: border-color 0.3s ease;
        }

        .contact-form input:focus,
        .contact-form select:focus,
        .contact-form textarea:focus {
            outline: none;
            border-color: var(--accent-color);
        }

        /* Footer */
        .footer {
            background: var(--text-dark);
            color: white;
            padding: 4rem 0 2rem;
            margin-top: 4rem;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 3rem;
            margin-bottom: 2rem;
        }

        .footer h3, .footer h4 {
            font-family: 'Crimson Text', serif;
            color: var(--sage-green);
            margin-bottom: 1rem;
        }

        .footer a {
            color: white;
            text-decoration: none;
            display: block;
            margin-bottom: 0.5rem;
            transition: color 0.3s ease;
        }

        .footer a:hover {
            color: var(--sage-green);
        }

        .social-links {
            display: flex;
            gap: 1rem;
        }

        .social-links a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            background: var(--accent-color);
            border-radius: 50%;
            transition: transform 0.3s ease;
        }

        .social-links a:hover {
            transform: translateY(-3px);
        }

        .footer-bottom {
            border-top: 1px solid var(--secondary-color);
            padding-top: 2rem;
            text-align: center;
            opacity: 0.8;
        }

        /* Mobile Menu */
        .mobile-menu-toggle {
            display: none;
            background: none;
            border: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--text-dark);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .mobile-menu-toggle {
                display: block;
            }

            .nav-menu {
                display: none;
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background: white;
                flex-direction: column;
                padding: 2rem;
                box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            }

            .nav-menu.active {
                display: flex;
            }

            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }

            .newsletter-form {
                flex-direction: column;
            }

            .services-grid,
            .events-grid,
            .donation-tiers,
            .features-grid,
            .testimonials-grid {
                grid-template-columns: 1fr;
            }

            .contact-content {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="#" class="logo" onclick="showPage('home')">Shronda Jeanine & Company</a>
            <button class="mobile-menu-toggle" onclick="toggleMobileMenu()">☰</button>
            <ul class="nav-menu" id="navMenu">
                <li><a href="#" class="nav-link" onclick="showPage('home')">Home</a></li>
                <li><a href="#" class="nav-link" onclick="showPage('about')">About</a></li>
                <li><a href="#" class="nav-link" onclick="showPage('services')">Services</a></li>
                <li><a href="#" class="nav-link" onclick="showPage('events')">Events</a></li>
                <li><a href="#" class="nav-link" onclick="showPage('testimonials')">Testimonials</a></li>
                <li><a href="#" class="nav-link" onclick="showPage('support')">Support Us</a></li>
                <li><a href="#" class="nav-link" onclick="showPage('newsletter')">Newsletter</a></li>
                <li><a href="#" class="nav-link" onclick="showPage('contact')">Contact</a></li>
                <li><a href="#" class="nav-link crisis-btn" onclick="accessCrisisSupport()">Crisis Support</a></li>
            </ul>
        </div>
    </nav>

    <!-- Home Page -->
    <div id="home" class="page active">
        <section class="hero">
            <div class="hero-content container">
                <h1>Shronda Jeanine & Company</h1>
                <p class="hero-subtitle">A holistic approach to healing from addiction and trauma, creating pathways to recovery through community, compassion, and connection in Arizona.</p>
                <div class="cta-buttons">
                    <a href="#" class="btn btn-primary" onclick="showPage('services')">Explore Services</a>
                    <a href="#" class="btn btn-secondary" onclick="showPage('events')">Join Community</a>
                </div>
            </div>
        </section>

        <div class="container">
            <div class="affirmation-container">
                <p id="daily-affirmation">"You are worthy of healing and peace."</p>
            </div>
        </div>

        <div class="features">
            <div class="container">
                <h3>Our Approach to Healing</h3>
                <div class="features-grid">
                    <div class="feature-card">
                        <i class="fas fa-users"></i>
                        <h4>Group Healing Sessions</h4>
                        <p>Connect with others on similar healing journeys in safe, supportive online environments.</p>
                    </div>
                    <div class="feature-card">
                        <i class="fas fa-heart"></i>
                        <h4>Holistic Coaching</h4>
                        <p>Personalized wellness coaching and virtual health consulting services tailored to your unique needs.</p>
                    </div>
                    <div class="feature-card">
                        <i class="fas fa-hands-helping"></i>
                        <h4>Community Support</h4>
                        <p>Join our healing community events, meditation sessions, and community garden projects.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- About Page -->
    <div id="about" class="page">
        <div class="page-header">
            <div class="container">
                <h2>About Our Mission</h2>
                <p>Serving homeless and underserved individuals and families in Phoenix, Arizona</p>
            </div>
        </div>
        
        <div class="container">
            <div class="contact-content">
                <div class="contact-info">
                    <h3>Our Mission</h3>
                    <p>At Shronda Jeanine & Company, we are dedicated to providing compassionate, holistic healing services that nurture mind, body, and spirit. Our community action program focuses on serving homeless and underserved individuals and families in Phoenix, Arizona.</p>
                    
                    <h3 style="margin-top: 2rem;">Our Approach</h3>
                    <ul style="list-style: none; padding-left: 0;">
                        <li style="margin-bottom: 1rem;"><i class="fas fa-check" style="color: var(--sage-green); margin-right: 10px;"></i>Holistic wellness coaching and virtual health consulting</li>
                        <li style="margin-bottom: 1rem;"><i class="fas fa-check" style="color: var(--sage-green); margin-right: 10px;"></i>Community-centered healing approach</li>
                        <li style="margin-bottom: 1rem;"><i class="fas fa-check" style="color: var(--sage-green); margin-right: 10px;"></i>Support for addiction and trauma recovery</li>
                        <li style="margin-bottom: 1rem;"><i class="fas fa-check" style="color: var(--sage-green); margin-right: 10px;"></i>Creating spaces that add value to the community</li>
                        <li style="margin-bottom: 1rem;"><i class="fas fa-check" style="color: var(--sage-green); margin-right: 10px;"></i>Monthly celebrations and community connections</li>
                    </ul>
                </div>
                <div class="contact-form" style="background: var(--soft-gray);">
                    <h3 style="color: var(--accent-color); margin-bottom: 2rem;">Business Registration</h3>
                    <p><strong>Shronda Jeanine & Company</strong></p>
                    <p>Registered with the State of Arizona</p>
                    <a href="https://ecorp.azcc.gov/BusinessSearch" target="_blank" style="color: var(--accent-color); text-decoration: none; margin-top: 1rem; display: inline-block;">
                        <i class="fas fa-external-link-alt"></i> Verify with Arizona Secretary of State
                    </a>
                    
                    <div style="margin-top: 3rem; padding: 2rem; background: white; border-radius: 10px; border-left: 4px solid var(--sage-green);">
                        <h4 style="color: var(--sage-green); margin-bottom: 1rem;">Our Focus</h4>
                        <p>We specialize in serving homeless and underserved individuals and families, creating pathways to healing through community support, holistic approaches, and sustainable community development.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Services Page -->
    <div id="services" class="page">
        <div class="page-header">
            <div class="container">
                <h2>Healing Services</h2>
                <p>Comprehensive support tailored to your journey of recovery and growth</p>
            </div>
        </div>
        
        <div class="container">
            <div class="services-grid">
                <div class="service-card">
                    <i class="fas fa-laptop"></i>
                    <h3>Online Group Sessions</h3>
                    <p>Connect with others on similar healing journeys in a safe, supportive online environment. Our facilitate
